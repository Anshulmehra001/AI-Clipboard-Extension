{
  "name": "ai-clipboard-extension",
  "version": "1.0.0",
  "description": "Chrome extension for AI-powered clipboard text transformation",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "setupFilesAfterEnv": ["<rootDir>/test/setup.js"],
    "testMatch": ["<rootDir>/test/**/*.test.js"]
  }
}